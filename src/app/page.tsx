export default function Home() {
  return (
    <main className="flex min-h-[calc(100vh-72px)] items-center justify-center px-5">
      {/* TODO: homepage sections build in order — Hero next */}
      <p className="font-mono text-sm tracking-[0.04em] text-muted">
        {"// homepage sections coming next"}
      </p>
    </main>
  );
}
