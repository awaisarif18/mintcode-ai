import Hero from "@/components/sections/Hero";

export default function Home() {
  return (
    <main>
      <Hero />
      {/* TODO: homepage sections build in order — Trust strip next */}
    </main>
  );
}
