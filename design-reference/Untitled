Update src/content/caseStudies.ts: replace the TradeSync Pro placeholder fields with this approved copy. Descriptor: "Real-time MT5 copy-trading platform." Headline result: "Eliminated manual position-sizing errors at near-zero infrastructure cost." Challenge: the MT5 copy-trading sizing problem — mirroring a master account's trades across many follower accounts in real time, where each account has a different balance, leverage and risk, so manual position-sizing is slow and error-prone, and off-the-shelf copiers are expensive or infra-heavy. Solution: a three-layer microservices system — lightweight Python desktop clients on each MT5 terminal stream trade events; a NestJS backend calculates the correct per-account position size and propagates trades in real time; a Next.js dashboard for control and monitoring; sizing is now automatic and deterministic, architected to run lean. Tech stack: Next.js, NestJS, Python (MT5 desktop clients), Microservices, PostgreSQL, AWS. Results (verifiable only): eliminated manual position-sizing errors; near-zero infrastructure cost. For the timeline, use these phase names with TODO durations — do NOT invent week counts: Discovery & architecture, Real-time sync engine, Per-account position-sizing, Dashboard & monitoring, Testing & hardening, Launch. Keep the status: 'published' flag. Don't touch the Chronic Pain Free entry — it stays the in-progress placeholder